name: Build and Deploy

on:
  push:
    branches:
      - master
    paths-ignore:
      - 'README.md'
      - 'LICENSE'
      - '.gitignore'

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Install Typst
        run: |
          curl -fsSL https://github.com/typst/typst/releases/download/v0.14.0/typst-x86_64-unknown-linux-musl.tar.xz -o typst.tar.xz
          tar -xf typst.tar.xz
          sudo mv typst-x86_64-unknown-linux-musl/typst /usr/local/bin/
          typst --version
      
      - name: Install fonts
        run: |
          # Install Chinese fonts for proper rendering
          sudo apt-get update
          sudo apt-get install -y fonts-noto-cjk fonts-noto-cjk-extra
          # Install LXGW WenKai font
          mkdir -p ~/.local/share/fonts
          curl -fsSL https://github.com/lxgw/LxgwWenKai/releases/download/v1.330/LXGWWenKai-Regular.ttf -o ~/.local/share/fonts/LXGWWenKai-Regular.ttf || true
          fc-cache -f -v || true
      
      - name: Extract version from index.typ
        id: version
        run: |
          VERSION=$(grep -oP '#let VERSION = "\K[^"]+' index.typ)
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Version extracted: $VERSION"
      
      - name: Build PDFs
        run: |
          # Build PDF without answers
          typst compile index.typ "高等数学习题册上册.pdf"
          
          # Build PDF with answers
          typst compile --input SHOW_ANSWER=true index.typ "高等数学习题册上册_带答案.pdf"
          
          # Verify files were created
          ls -lh *.pdf
          
          # Save PDFs to a temporary location
          mkdir -p /tmp/pdfs
          cp "高等数学习题册上册.pdf" "高等数学习题册上册_带答案.pdf" /tmp/pdfs/
      
      - name: Install Python dependencies
        run: |
          python3 -m pip install --upgrade pip
      
      - name: Export HTML
        run: |
          # Export both versions (without and with answers)
          python3 scripts/export_html.py --both
          
          # Verify HTML files were created
          echo "Files in index/:"
          find index -type f -name "*.html" | head -10
          echo "Files in index_ans/:"
          find index_ans -type f -name "*.html" | head -10
          
          # Save HTML to temporary location
          mkdir -p /tmp/html_export
          cp -r index /tmp/html_export/
          cp -r index_ans /tmp/html_export/
      
      - name: Fetch web branch
        run: |
          git fetch origin web
          git checkout web
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
      
      - name: Update index.html version
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          sed -i "s/<span class=\"version-value\" id=\"version\">v[0-9.]*</<span class=\"version-value\" id=\"version\">$VERSION</g" index.html
          
          # Verify the change
          grep "version-value" index.html
      
      - name: Copy PDFs to web branch
        run: |
          cp /tmp/pdfs/*.pdf .
      
      - name: Copy HTML to web branch
        run: |
          # Copy HTML exports to web branch
          cp -r /tmp/html_export/index .
          cp -r /tmp/html_export/index_ans .
      
      - name: Commit and push to web branch
        run: |
          git add index.html index/ index_ans/ "高等数学习题册上册.pdf" "高等数学习题册上册_带答案.pdf"
          
          # Only commit if there are changes
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Auto-update: version ${{ steps.version.outputs.version }} [skip ci]"
            git push origin web
          fi
      
      - name: Switch back to master for release
        run: |
          git checkout master
      
      - name: Restore PDFs for release
        run: |
          cp /tmp/pdfs/*.pdf .
      
      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ steps.version.outputs.version }}
          name: Release ${{ steps.version.outputs.version }}
          body: |
            ## 高等数学习题册（上册）${{ steps.version.outputs.version }}
            
            自动构建和发布
            
            ### 下载
            - 📖 高等数学习题册上册.pdf (无答案版)
            - 📖 高等数学习题册上册_带答案.pdf (带答案版)
            
            ### 在线预览
            访问：https://dtce.xihale.top
          files: |
            高等数学习题册上册.pdf
            高等数学习题册上册_带答案.pdf
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
