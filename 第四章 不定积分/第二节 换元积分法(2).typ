#import "../template.typ": *

#section[第二节 换元积分法(2)]
#subsection[一、填空题]
1. 如果被积函数中含有 $sqrt(a^2 - x^2)$，可做代换将根式化去，此时 $dif x =$ #blank(length: 2.5em)
2. 如果被积函数中含有 $sqrt(a^2 + x^2)$，可做代换将根式化去，此时 $dif x =$ #blank(length: 2.5em)
3. 如果被积函数中含有 $sqrt(x^2 - a^2)$，可做代换将根式化去，此时 $dif x =$ #blank(length: 2.5em)
#subsection[二、计算题]
4. 计算下列不定积分：\
   (1) $integral (dif x)/(x sqrt(1 + x^2))$；
   #answer-space
   (2) $integral sin sqrt(x) dif x$；
   #answer-space
   (3) $integral (x^2)/(sqrt(a^2 - x^2)) dif x$；
   #answer-space
   (4) $integral (dif x)/(1 + sqrt(2x))$；
   #answer-space
   (5) $integral (dif x)/(sqrt((x^2 + 1)^3))$；
   #answer-space
   (6) $integral (dif x)/(x + sqrt(1 - x^2))$；
   #answer-space
   (7) $integral (sqrt(x^2 - 4))/x dif x$。
   #answer-space
5.(附加题)计算下列不定积分：\
   (1) $integral (x^3 + 1)/((x^2 + 1)^2) dif x$；
   #answer-space
   (2) $integral (dif x)/(x^100 + x)$。
   #answer-space
