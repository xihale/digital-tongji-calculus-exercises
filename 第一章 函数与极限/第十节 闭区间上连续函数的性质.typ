#import "../template.typ": *

#section[第十节 闭区间上连续函数的性质]
#subsection[一、证明题]
1. 证明: 方程  $x^5 - 3x = 1$  至少有一个根介于 1 和 2 之间.
   #answer-process[
   设函数 $f(x) = x^5 - 3x - 1$，则 $f(x)$ 在 $[1, 2]$ 上连续。
   
   计算 $f(1)$ 和 $f(2)$：
   - $f(1) = 1^5 - 3 * 1 - 1 = 1 - 3 - 1 = -3 < 0$
   - $f(2) = 2^5 - 3 * 2 - 1 = 32 - 6 - 1 = 25 > 0$
   
   由于 $f(x)$ 在 $[1, 2]$ 上连续，且 $f(1) < 0 < f(2)$，根据中间值定理，存在 $c in (1, 2)$，使得 $f(c) = 0$，即 $c^5 - 3c = 1$。
   
   因此，方程 $x^5 - 3x = 1$ 至少有一个根介于 1 和 2 之间。
   ]

2. 证明: 方程  $x = a sin x + b (a > 0, b > 0)$  至少有一个正根, 并且它不超过  $a + b$ .
   #answer-process[
   设函数 $f(x) = x - a sin x - b$，则 $f(x)$ 在 $[0, a+b]$ 上连续。
   
   计算 $f(0)$ 和 $f(a+b)$：
   - $f(0) = 0 - a sin 0 - b = -b < 0$ (因为 $b > 0$)
   - $f(a+b) = (a+b) - a sin(a+b) - b = a - a sin(a+b) = a(1 - sin(a+b))$
   
   由于 $sin(a+b) <= 1$，所以 $1 - sin(a+b) >= 0$，因此 $f(a+b) = a(1 - sin(a+b)) >= 0$。
   
   如果 $f(a+b) = 0$，则 $a+b$ 就是方程的根。
   如果 $f(a+b) > 0$，由于 $f(x)$ 在 $[0, a+b]$ 上连续，且 $f(0) < 0 < f(a+b)$，根据中间值定理，存在 $c in (0, a+b)$，使得 $f(c) = 0$，即 $c = a sin c + b$。
   
   因此，方程 $x = a sin x + b$ 至少有一个正根，并且它不超过 $a + b$。
   ]

3. 设函数  $f(x)$  在区间  $[0,1]$  上连续，且对  $[0,1]$  上任一点  $x$  有  $0 <= f(x) <= 1$ . 试证：在  $[0,1]$  上必存在一点  $c$ ，使得  $f(c) = c$  ( $c$  称为函数  $f(x)$  的不动点).
   #answer-process[
   设函数 $g(x) = f(x) - x$，则 $g(x)$ 在 $[0,1]$ 上连续。
   
   计算 $g(0)$ 和 $g(1)$：
   - $g(0) = f(0) - 0 = f(0) >= 0$ (因为 $0 <= f(x) <= 1$)
   - $g(1) = f(1) - 1 <= 0$ (因为 $0 <= f(x) <= 1$)
   
   如果 $g(0) = 0$，则 $f(0) = 0$，即 $c = 0$ 是不动点。
   如果 $g(1) = 0$，则 $f(1) = 1$，即 $c = 1$ 是不动点。
   如果 $g(0) > 0$ 且 $g(1) < 0$，由于 $g(x)$ 在 $[0,1]$ 上连续，根据中间值定理，存在 $c in (0,1)$，使得 $g(c) = 0$，即 $f(c) = c$。
   
   因此，在 $[0,1]$ 上必存在一点 $c$，使得 $f(c) = c$。
   ]

4. 设函数  $f(x)$  在区间  $[a, b]$  上连续， $a < x_1 < x_2 < dots < x_n < b (n >= 3)$ ，证明：在区间  $(x_1, x_n)$  内至少存在一点  $xi$ ，使得  $f(xi) = (f(x_1) + f(x_2) + dots + f(x_n))/n$ .
   #answer-process[
   设 $m = min{f(x_1), f(x_2), dots, f(x_n)}$，$M = max{f(x_1), f(x_2), dots, f(x_n)}$，则 $m <= f(x_i) <= M$ 对所有 $i = 1, 2, dots, n$ 成立。
   
   因此，$m <= (f(x_1) + f(x_2) + dots + f(x_n))/n <= M$。
   
   由于 $f(x)$ 在 $[a, b]$ 上连续，所以在 $[x_1, x_n]$ 上也连续。根据介值定理，对于介于 $m$ 和 $M$ 之间的任何值，都存在 $[x_1, x_n]$ 中的点使得 $f(x)$ 等于该值。
   
   特别地，对于 $(f(x_1) + f(x_2) + dots + f(x_n))/n$，它介于 $m$ 和 $M$ 之间，所以存在 $xi in [x_1, x_n]$，使得 $f(xi) = (f(x_1) + f(x_2) + dots + f(x_n))/n$。
   
   由于 $m$ 和 $M$ 分别是 $f(x)$ 在点 $x_1, x_2, dots, x_n$ 上的最小值和最大值，所以 $xi$ 不可能等于 $x_1$ 或 $x_n$（除非所有 $f(x_i)$ 都相等，此时 $xi$ 可以是 $[x_1, x_n]$ 中的任意点）。因此，$xi in (x_1, x_n)$。
   ]