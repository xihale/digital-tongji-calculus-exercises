#import [../template.typ]: section, subsection, choices, answer-space, blank

#section[总习题四]
#subsection[一、选择题]
1. 若函数  $f(x)$  在区间  $(a, b)$  内连续，则在  $(a, b)$  内  $f(x)$  ( ).
   #v(0.5em)
   #choices(
   必有导函数,
   必有原函数,
   必有界,
   必有极限
   )
2. 若  $F'(x) = f(x), v a r p h i'(x) = f(x)$ ，则  $integral f(x) , dif x = (quad)$ .
   #v(0.5em)
   #choices(
   $F(x)$,
   $varphi(x)$,
   $varphi (x) + C$,
   $F(x) + varphi (x) + C$
   )
3.下列式子中正确的是( )
   #v(0.5em)
   #choices(
   $d[integral f(x) dif x] = f(x)$,
   $dif/d x[integral f(x) diff x] = f(x) diff x$,
   $integral dif f(x) = f(x)$,
   $integral dif f(x) = f(x) + C$
   )
4. 设函数  $f(x) = e^(-x)$ ，则  $integral (f(ln x))/x , dif x = (quad)$ .
   #v(0.5em)
   #choices(
   $1/x + C$,
   $ln x + C$,
   $-1/x + C$,
   $-ln x + C$
   )
5.  $integral (dif x)/(sqrt(x(1 - x))) = (quad)$
   #v(0.5em)
   #choices(
   $1/2 arcsin sqrt(x) + C$,
   $arcsin sqrt(x) + C$,
   $2 arcsin (2x - 1) + C$,
   $arcsin (2x - 1) + C$
   )
#subsection[二、填空题]
6.  $integral (1 - sin^2(x/2)) dif x =$  
7. 若  $e^x$  是函数  $f(x)$  的一个原函数, 则  $integral x^2 f(ln x) dif x = underline(quad)$ .  
8. 设  $F'(x) = f(x)$ , 则  $integral f(a x + b) , dif x = underline(quad)$ .  
9. 设  $integral x f(x) dif x = arcsin x + C$ ，则  $integral (dif x)/(f(x)) = #blank()$ . 
10. 若  $integral x f(x) dif x = x sin x - integral sin x diff x$ ，则  $f(x) =$  #blank().
#subsection[三、计算题]
11. 计算下列不定积分：
    (1)  $integral cos sqrt(x) , dif x$ ;  
    #answer-space
    (2)  $integral (sin 2x)/(cos^4 x - sin^4 x) , dif x$;
    #answer-space
    (3)  $integral (dif x)/(cos^2 x root(4, tan x))$ ;  
    #answer-space
    (4)  $integral (x ln(1 + x^2))/(1 + x^2) dif x$.
    #answer-space
12. 设函数  $f(sin^2 x) = x/(sin x)$ ，求  $integral (sqrt(x))/(sqrt(1 - x)) f(x) , dif x$.
    #answer-space
13. 已知函数  $f(x)$  的一个原函数为  $ln^2 x$ ，求  $integral x f'(x) , dif x$ .
    #answer-space
#align(center)[
总习题四结束
]