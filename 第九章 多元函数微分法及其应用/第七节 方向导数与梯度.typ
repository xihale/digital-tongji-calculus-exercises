// 数据层：只放内容。公式一律行内。
// 由下册自动转换（git 完整题量 + HEAD 解析优先）；请抽查选项切分。
#import "../lib/math.typ": *
#import "../lib/render.typ": book-section

#let section-title = [第七节 方向导数与梯度]

#let problems = (
  (
    kind: "blank",
    kind-title: [一、填空题],
    stem: [
      设函数 $z = x^2 - y^2$，求其在点 $(1, 1)$ 处沿与 $x$ 轴正向成 $(3 pi)/(4)$ 方向的方向导数为
    ],
    answer: [$-2 sqrt(2)$],
    solution: [
      先求梯度： \
      $bold(grad) z=(z_x,z_y)=(2x,-2y)$， \
      所以在 $(1,1)$ 处 \
      $bold(grad) z(1,1)=(2,-2)$。 \
      方向与 $x$ 轴正向成 $(3pi)/(4)$，对应单位方向向量 \
      $bold(e)=(cos (3pi)/4,sin (3pi)/4)=(-(sqrt(2))/(2),(sqrt(2))/(2))$。 \
      方向导数公式： \
      $D_(bold(e)) z(1,1)=bold(grad) z(1,1) dot bold(e)$。 \
      代入得 \
      $D_(bold(e)) z(1,1)=(2,-2) dot (-(sqrt(2))/(2),(sqrt(2))/(2))$ \
      $=-sqrt(2)-sqrt(2)=-2 sqrt(2)$。
    ],
  ),
  (
    kind: "blank",
    stem: [
      设函数 $f(x, y, z) = x^2 + 2 y^2 + 3 z^3 + x y + 3 x - 2 y - 6 z$，则 $bold(grad) f(0,0,0) =$
    ],
    answer: [$(3, -2, -6)$],
    solution: [
      分别求偏导： \
      $f_x=2x+y+3$， \
      $f_y=x+4y-2$， \
      $f_z=9z^2-6$。 \
      所以 \
      $bold(grad) f=(f_x,f_y,f_z)=(2x+y+3, x+4y-2, 9z^2-6)$。 \
      在 $(0,0,0)$ 处代入： \
      $f_x(0,0,0)=3$， \
      $f_y(0,0,0)=-2$， \
      $f_z(0,0,0)=-6$。 \
      故 \
      $bold(grad) f(0,0,0)=(3,-2,-6)$。
    ],
  ),
  (
    kind: "compute",
    kind-title: [二、计算题],
    stem: [
      设函数 $u = x^2 + y^2 + z^2$，求： \
      (1) $bold(grad) u(1, 1, 1)$； \
      (2) $((partial u)/(partial l)) |_((1, 1, 1))$，其中方向 $l$ 从点 $(1, 1, 1)$ 到点 $(2, 3, 3)$。
    ],
    solution: [
      (1) 先求梯度： \
      $bold(grad) u=(u_x,u_y,u_z)=(2x,2y,2z)$。 \
      在 $(1,1,1)$ 处有 \
      $bold(grad) u(1,1,1)=(2,2,2)$。 \
      (2) 方向 $l$ 从 $(1,1,1)$ 指向 $(2,3,3)$， \
      方向向量 \
      $bold(v)=(2-1,3-1,3-1)=(1,2,2)$， \
      模长 $|bold(v)|=3$， \
      故单位方向向量 \
      $bold(e_l)=((1)/(3),(2)/(3),(2)/(3))$。 \
      方向导数 \
      $((partial u)/(partial l))|_(1,1,1)=bold(grad)u(1,1,1) dot bold(e_l)$ \
      $=(2,2,2) dot ((1)/(3),(2)/(3),(2)/(3))=(10)/(3)$。
    ],
  ),
  (
    kind: "compute",
    stem: [
      设函数 $f(x, y) = sqrt(x^2 + y^4)$，求： \
      (1) $bold(grad) f(1, -1)$； \
      (2) $(partial f)/((partial l)|_(1, -1))$，其中方向 $l$ 与 $x$ 轴正向成 $(pi)/(4)$ 角； \
      (3) $(partial f)/((partial l)|_(1, -1))$，其中方向 $l$ 与 $bold(grad) f(1,1)$ 同向。
    ],
    solution: [
      (1) $((( sqrt(2))/(2)), - sqrt(2))$ ; \
      (2) $-((1)/(2))$ ; \
      (3) $-((3 sqrt(10))/(10))$ .

      先求梯度。设 \
      $g=x^2+y^4$，则 \
      $f=sqrt(g)$， \
      $f_x=(x)/(sqrt(x^2+y^4)), f_y=(2y^3)/(sqrt(x^2+y^4))$。 \
      在 $(1,-1)$ 处有 \
      $sqrt(x^2+y^4)=sqrt(2)$， \
      故 \
      $bold(grad)f(1,-1)=((1)/(sqrt(2)),-(2)/(sqrt(2)))=((sqrt(2))/(2),-sqrt(2))$。 \
      (2) 与 $x$ 轴成 $(pi)/(4)$ 的单位向量 \
      $bold(e_1)=(cos pi/4,sin pi/4)=((sqrt(2))/(2),(sqrt(2))/(2))$。 \
      方向导数 \
      $D_(bold(e_1))f=bold(grad)f dot bold(e_1)$ \
      $=((sqrt(2))/(2),-sqrt(2)) dot ((sqrt(2))/(2),(sqrt(2))/(2))=-(1)/(2)$。 \
      (3) 先算 \
      $bold(grad)f(1,1)=((1)/(sqrt(2)),sqrt(2))$， \
      其模为 \
      $sqrt((1)/(2)+2)=sqrt((5)/(2))$。 \
      同向单位向量 \
      $bold(e_2)=(bold(grad)f(1,1))/(|bold(grad)f(1,1)|)=((1)/(sqrt(5)),(2)/(sqrt(5)))$。 \
      故 \
      $D_(bold(e_2))f(1,-1)=bold(grad)f(1,-1) dot bold(e_2)$ \
      $=((sqrt(2))/(2),-sqrt(2)) dot ((1)/(sqrt(5)),(2)/(sqrt(5)))=-(3)/(sqrt(10))$ \
      $=-(3sqrt(10))/(10)$。
    ],
  ),
  (
    kind: "compute",
    stem: [
      如果可微函数 $f(x, y)$ 在点 $(1, 2)$ 处沿从点 $(1, 2)$ 到点 $(2, 2)$ 的方向的方向导数为 2，沿从点 $(1, 2)$ 到点 $(1, 1)$ 的方向的方向导数为 $-2$，求： \
      (1) $bold(grad) f(1, 2)$； \
      (2) $((partial f)/(partial l)) |_(1, 2)$，其中方向 $l$ 从点 $(1, 2)$ 到点 $(4, 6)$。
    ],
    solution: [
      $bold(grad)f(1,2)=(2,2)$，$D_l f(1,2)=(14)/(5)$。

      由题意，沿从 $(1,2)$ 到 $(2,2)$ 的方向有 \
      $ D_(bold(e_1)) f(1,2)=bold(grad)f(1,2) dot (1,0)=f_x(1,2)=2 $ \
      沿从 $(1,2)$ 到 $(1,1)$ 的方向有 \
      $ D_(bold(e_2)) f(1,2)=bold(grad)f(1,2) dot (0,-1)=-f_y(1,2)=-2 $ \
      所以 $f_y(1,2)=2$，从而 \
      $ bold(grad)f(1,2)=(2,2) $ \
      再由方向 $l$ 从 $(1,2)$ 指向 $(4,6)$，其单位向量为 \
      $ bold(e_l)=((3)/(5),(4)/(5)) $ \
      故所求方向导数为 \
      $ D_l f(1,2)=bold(grad)f(1,2) dot bold(e_l) =(2,2) dot ((3)/(5),(4)/(5))=(14)/(5) $
    ],
  ),
  (
    kind: "compute",
    stem: [
      已知曲面 $2 x^2 + 3 y^2 + z^2 = 6$ 在点 $P(1,1,1)$ 处指向外侧的法向量为 $bold(n)$ , 求函数 $u = (( sqrt(6 x^2 + 8 y^2))/(z))$ 在点 $P$ 处沿方向 $bold(n)$ 的方向导数.
    ],
    solution: [
      $D_(bold(n))u(P)=(11)/(7)$。

      先求外法向量。令 \
      $ F(x,y,z)=2x^2+3y^2+z^2-6 $ \
      则在 $P(1,1,1)$ 处有 \
      $grad F(P)=(4,6,2)$，$bold(n)=(2,3,1)/(sqrt(14))$ \
      再求函数 \
      $ u(x,y,z)=(sqrt(6x^2+8y^2))/(z) $ \
      的梯度。分别有 \
      $ u_x=(6x)/(z sqrt(6x^2+8y^2)) $ \
      $ u_y=(8y)/(z sqrt(6x^2+8y^2)) $ \
      $ u_z=-(sqrt(6x^2+8y^2))/(z^2) $ \
      代入点 $P$，得到 \
      $ grad u(P)=((6)/(sqrt(14)),(8)/(sqrt(14)),-sqrt(14)) $ \
      所以方向导数为 \
      $
        D_(bold(n))u(P)=grad u(P) dot bold(n) =(((6)/(sqrt(14)),(8)/(sqrt(14)),-sqrt(14)) dot (2,3,1))/(sqrt(14)) =(11)/(7)
      $
    ],
  ),
  (
    kind: "proof",
    kind-title: [三、证明题],
    stem: [
      （附加题）证明：函数 $f(x, y) = root(3, x y)$ 在点(0,0)处连续且可偏导，但除沿坐标轴的方向外，在点(0,0)处沿其他方向的方向导数都不存在。
    ],
    solution: [
      （1）连续性： \
      因为 \
      $ f(x,y)=(x y)^((1)/(3)) $ \
      当 $(x,y) arrow (0,0)$ 时有 $x y arrow 0$，故 $f(x,y) arrow 0=f(0,0)$，所以在原点连续。 \
      （2）偏导数：由定义 \
      $ f_x(0,0)=lim_(h->0) (f(h,0)-f(0,0))/h=0 $ \
      $ f_y(0,0)=lim_(h->0) (f(0,h)-f(0,0))/h=0 $ \
      因而两偏导都存在。 \
      （3）方向导数：设方向向量为 $l=(m,n)$，则 \
      $
        D_l f(0,0) =lim_(t->0) (f(t m,t n)-f(0,0))/t =lim_(t->0) ((m n t^2)^((1)/(3)))/t =(m n)^((1)/(3)) lim_(t->0) 1/(t^((1)/(3)))
      $ \
      当 $m n != 0$ 时，上式极限发散，方向导数不存在；当 $m n = 0$（即沿坐标轴方向）时，分子恒为 0，方向导数为 0。 \
      综上，函数在 $(0,0)$ 处连续且可偏导，但除沿坐标轴方向外，其余方向导数都不存在。
    ],
  ),
)

#book-section(section-title, problems)
